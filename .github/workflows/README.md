# .github/workflows

Carpeta de GitHub Actions (CI/CD).

No modifica directamente el comportamiento conversacional del agente, pero sí su entorno de validación (build/test/lint/security) vía pipelines.
